import type { Metadata } from "next";
import "./globals.css";
import { AuthProvider } from "@/context/AuthContext";
import { ThemeProvider } from "@/context/ThemeContext";

export const metadata: Metadata = {
  title: "BioAttend - Biometric Student Attendance System",
  description: "Biometric student attendance management system with fingerprint verification for educational institutions",
  keywords: ["attendance", "management", "biometric", "fingerprint", "university", "education"],
  authors: [{ name: "Final Year Project" }],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>
        <ThemeProvider>
          <AuthProvider>
            {children}
          </AuthProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
